"use strict";

/**
 * Messages to display to user.
 */
const enum UIMessages {
  invalidInputMarkdownPath = "Path to Markdown file was not valid. Trying with currently open file instead.",
  invalidInputMarkdownFile = "Input file is not in Markdown.",
  noValidMarkdownFile = "No valid Markdown file.",
}

export default UIMessages;
